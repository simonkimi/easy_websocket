from distutils.core import setup

setup(name="easy_websocket",
      version="2。0",
      description="An easy websocket server and client based on websockets",
      author="simonkimi",
      py_modules=['easy_websocket'],
      requires=['websockets'])
